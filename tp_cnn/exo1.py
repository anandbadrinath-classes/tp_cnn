# Exercice 1
# Impléméenter la fonction filter_detections, cette fonction prend en entrée les résultats de la détection par
# YOLO et retourne uniquement ceux des classes "person" et "suitcase" dont le score est supérieur à 0.5
def filter_detections(boxes, scores, classes, nums, class_names):
    return boxes, scores, classes, nums
